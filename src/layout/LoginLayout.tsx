import { Outlet } from 'react-router-dom';

const LoginLayout = () => {
    return (
        <div>
            LoginLayout
            <Outlet />
        </div>
    );
};

export default LoginLayout;
